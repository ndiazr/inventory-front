/*
 * CategoriesPage Messages
 *
 * This contains all the text for the CategoriesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CategoriesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CategoriesPage container!',
  },
});
