/*
 * StockTable Messages
 *
 * This contains all the text for the StockTable container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StockTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StockTable container!',
  },
});
